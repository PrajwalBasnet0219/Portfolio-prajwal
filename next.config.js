/** @type {import('next').NextConfig} */
// next.config.js
const nextConfig = {
  // Remove or comment out output: 'export'
  // output: 'export',
  
  images: {
    unoptimized: true,
  },
};

module.exports = nextConfig;
